% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/views.R, R/views_new.R
\name{read_package_views}
\alias{read_package_views}
\title{Read all package views}
\usage{
read_package_views(packages, path, package_type)

read_package_views(packages, path, package_type)
}
\arguments{
\item{packages}{vector of package names}

\item{path}{character}

\item{package_type}{character in .PACKAGE_TYPES}

\item{branch}{character "release" or "devel"}
}
\value{
data.frame, all package view data, with list-columns for
fields such as Suggests/Depends/Imports/biocViews that have
multiple values in at least one package

data.frame, all existing package view data (0 rows if none of
\code{packages} has an existing file yet)
}
\description{
Reads existing package-view JSON files for the given \code{packages}. A
package with no existing file yet (i.e. it's never been propagated
before) is simply omitted from the result rather than raising an error
-- \code{changed_packages()} already treats a package's absence from this
output as "needs updating," and the JSON file itself gets created the
first time that package actually passes propagation criteria, via
\code{update_package_views()}.
}
\examples{
views <- read_package_views("devel", "software")

views <- read_package_views(c("BiocCheck", "a4Reporting"), "devel",
    "software")

}
