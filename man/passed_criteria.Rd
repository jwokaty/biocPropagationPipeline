% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/views.R, R/views_new.R
\name{passed_criteria}
\alias{passed_criteria}
\title{Passed packages VIEWS update}
\usage{
passed_criteria(changed, universe, r_version, os = c("linux", "win", "mac"))

passed_criteria(changed, universe, r_version, os = c("linux", "win", "mac"))
}
\arguments{
\item{changed}{character vector of package names to check (typically
the output of \code{changed_packages()})}

\item{universe}{character corresponding R Universe}

\item{r_version}{character R version}

\item{os}{(default: linux, win, mac) OSes to check}

\item{views}{data.frame representing VIEWS}
}
\value{
data.frame, one row per package source or binary

data.frame, one row per package/OS/arch job, joined against
propagation Status (TRUE for combinations that pass)
}
\description{
Wraps \code{BiocPropagate::propagate()} to report which \verb{(Package, OS, Arch)}
combinations currently meet propagation criteria -- i.e. which binaries
should actually be made publicly available. This is a SEPARATE concern
from whether a package gets a VIEWS entry at all: a VIEWS JSON should
exist for every package (normally created/refreshed for the whole repo
during a Bioconductor release), regardless of propagation status. This
function's output is meant for \code{write_propagation_status()}, not for
deciding which packages \code{update_package_views()} writes a file for.
}
\examples{
bu <- biocUniTools::uni_for_bioc("release")
views <- read_package_views(c("BiocCheck", "bedbaser"), "release",
    "software")
passed <- passed_criteria(views, bu$universe, bu$r_version)

bu <- biocUniTools::uni_for_bioc("release")
views <- read_package_views(c("BiocCheck", "bedbaser"), "release",
    "software")
universe_df <- biocUniTools::get_raw_uni_df(bu$universe)
changed <- changed_packages(views, universe_df)
status <- passed_criteria(changed, bu$universe, bu$r_version)

}
